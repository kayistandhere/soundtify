import { StorageReference, uploadBytes } from "firebase/storage";


const uploadSingleFile = async (ref: StorageReference, filePath: string, ): Promise<void> => {

};

export default uploadSingleFile;